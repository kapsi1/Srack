---
trigger: always_on
---

If you're trying to check the running application, first check and use existing server/client dev processes. By default dev client will be running on port 3000, server on PORT value from server/.env file. If those aren't found then run a new process.